# User permissions and permission history

`GET /users/[id]/permissions`, `PATCH /users/[id]/permissions` (replacesPUT), and `GET /users/[id]/permissions/audit`. Paths under /api. Positive numeric Int32 target or session-only me. All require global user:manage_permissions plus live target hierarchy; active bots remain superadmin. Self read allowed with global grant, self mutations always403 even superadmin humans. Unknown query400, invalid JSON400, strict body422, missing target/reference404, concurrency409, invalidcredentials401.

Permission resource GET accepts no query and returns data `{user:{id,username},permissions:[{id,key,description,defaultValue,maxValue,currentValue}]}`. Catalog sorted bykey; missing grants currentValue0 (does not apply defaultValue). Other-target reads audited resourceuser_permissions; self read no audit.

PATCH exact `{permissions:[{permissionId,value}]}` nonempty, unique numeric positiveInt32 IDs, integer values0..255 and <=Permission.maxValue. References preflight404 and unsupported catalog keys422. Partial update: omitted grants preserved, value0 deletes, equalvalues no-op. Non-superadmin cannot alter system:super_admin and can only change grants when both old/new values are strictly below their own corresponding permission level. All entries preflight before any writes. Target hierarchy uses user:manage_permissions independently. Bot superadmin can grant/revoke any valid catalog permission. Serializable transaction includes changed grants, per-permission legacy history and canonical audituser_permissions.updated. Snapshot only changed permission IDs and numericvalues. Response200 datanull; no-op no mutationaudit or notification. Profile event aftercommit safely.

Audit GET supports only action(GRANT|REVOKE|MODIFY),limit(default50cap100),cursor(descendingpositiveInt32); unknown/duplicates/invalid400. Dataarray `{id,action,oldValue,newValue,reason,createdAt:ISO-UTC,actor:{id,username}|null,actorType:user|bot|deleted_user,permission:{key,description}}`. Excludes request IP/user-agent metadata and avatar. Actual lookaheadnextCursor/string-or-null. Audits targetuser and returned human actors otherthan sessionactor; botall, no lookaheadactors; snapshotsnone. Required auditfailure withholds data. Empty other-target history still audits target access.

Schema: PermissionAuditLog.actor/actorId nullable; onDelete changed Cascade→SetNull so deleting a human actor preserves permission history (target/permission cascades unchanged). Bot history actorIdnull, internalmetadata actorTypebot/actorTokenId; canonicalApiAuditLog carries definitive token identity. Existing history with deleted actor is labelled deleted_user. Migration20260919010000_nullable_permission_audit_actor generated schema-to-schema, no real database touched. Prisma regenerated.

UserManagementClient and UserDetailClient use apiRequest/PATCH; PermissionAuditLog uses apiList cursor accumulation and handles null actors, drops metadata/avatar. LegacyPUT export removed.

Tests:28 focused unit tests pass;7 real Prisma integration cases cover live/session/bot rights, partial update/no-op/revocation, delegation bounds and all-entry preflight, maxValue, nullable bot actors/deleted-actor retention, UTC/privacy/paging, and rollback of legacy plus canonical audits. Central integration pending.
